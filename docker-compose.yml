version: "3.9"
services:
  app:
    build: .
    image: terraform-manager:local
    environment:
      - TFM_PORT=8890
      # - TFM_API_TOKEN=changeme  # uncomment to require API token for JSON endpoints
    ports:
      - "8890:8890"
    volumes:
      - ./knowledge:/app/knowledge
      - ./data:/app/data
    command: ["python", "-m", "api"]

